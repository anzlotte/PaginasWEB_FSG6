<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TuPaginaYA</title>
    <link rel="stylesheet" href="/css/estilosUsuario.css">
    <link rel="shortcut icon" href="/imagenes/typaginaya.png" type="image/x-icon">
</head>
 <!-- Se agrega los enlaces -->
 <header>
    <nav>
        <ul>
            <li><a href="/home" class="" type="button">Home</a></li>
            <li><a href="#home"></a></li>
            <li><a href="#services"></a></li>
            <li><a href="#portfolio"></a></li>
            <li><a href="#contact"></a></li>
        </ul>
    </nav>
</header>
<body>
    <!-- Contenedor principal del formulario -->
    <div class="form-container">
        <h1 class="form-title">Formulario de Clientes</h1>
        <div class="container">
     
    <div class="container">
        <div class="row mt-5">
            <div class="col-md-7">
                <table class="table table-bordered table-hover">
                    <thead>
                        <th>Id</th>
                        <th>Nombre</th>
                        <th>Correo</th>
                        <th>Telefono</th>
                        <th>Comentario</th>
                        <th>Acciones</th>
                    </thead>
                    <tbody>
                        <% if (data && data.length > 0) { %>
                            <% data.forEach((cliente, i) => { %>
                                <tr>
                                    <td><%= (i + 1) %></td>
                                    <td><%= cliente.nombre %></td>
                                    <td><%= cliente.correo %></td>
                                    <td><%= cliente.telefono %></td>
                                    <td><%= cliente.comentarios %></td>
                                    <td class="acciones">
                                        <a href="/update/<%= cliente.id %>" class="btn">Editar</a>
                                        <a href="/delete/<%= cliente.id %>" class="btn">Eliminar</a>
                                    </td>
                                </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="6">No hay clientes registrados.</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
                
                <!-- Paginación -->
                <div class="pagination">
                    <% if (currentPage > 1) { %>
                        <a href="/?page=<%= currentPage - 1 %>" class="prev">&laquo; Anterior</a>
                    <% } %>
                
                    <% for (let i = 1; i <= totalPages; i++) { %>
                        <a href="/?page=<%= i %>" class="<%= i === currentPage ? 'active' : '' %>"><%= i %></a>
                    <% } %>
                
                    <% if (currentPage < totalPages) { %>
                        <a href="/?page=<%= currentPage + 1 %>" class="next">Siguiente &raquo;</a>
                    <% } %>
                </div>
                
<footer>
    <div class="footer__container">
        <img class="tupaginaya__icon" src="/imagenes/tupagina.png" alt="tupaginaYA Logo">
        <div class="container__icons--footer">
            <div class="container-icons--footer">
            </div>
        <p class="">© 2024. All rights reserved</p>
    </div>
</footer>
</body>
</html>



